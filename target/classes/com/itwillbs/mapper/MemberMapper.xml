<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itwillbs.mapper.MemberMapper">
	<!-- login 로직 -->
	<select id="loginMember"
		parameterType="com.itwillbs.domain.MemberVO"
		resultType="com.itwillbs.domain.MemberVO">
		SELECT *
		FROM users
		WHERE user_id = #{user_id} AND password =
		#{password}
	</select>

	<!-- 회원가입 ID중복체크 -->
	<select id="checkUserIdExists" parameterType="String"
		resultType="int">
		SELECT COUNT(*) FROM users WHERE user_id = #{user_id}
	</select>
	
		<!-- 전화번호 중복 체크 -->
	<select id="checkPhoneExists" resultType="boolean">
		SELECT EXISTS(SELECT 1 FROM users WHERE phone = #{phone})
	</select>
	
	<!-- 이메일 중복 체크 -->
	<select id="checkEmailExists" parameterType="String" resultType="boolean">
	    SELECT EXISTS(SELECT 1 FROM users WHERE email = #{email})
	</select>
	
	<!-- 닉네임 중복 체크 -->
	<select id="checkNicknameExists" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM users WHERE nickname = #{nickname}
	</select>
	

	<insert id="insertMember">
		insert into users (user_id, role, password, username,
		email, gender,
		birth_date, phone, profile_image, nickname, termsAgree, privacyAgree, marketingAgree)
		values(#{user_id},
		#{role},#{password},#{username},#{email},#{gender},#{birthDate},#{phone},#{profileImage},#{nickname},
		 #{termsAgree}, #{privacyAgree}, #{marketingAgree}
		)
	</insert>

	<select id="getGenres" resultType="com.itwillbs.domain.GenresVO">
		SELECT genre_id AS genreId,
		genre_name AS genreName
		FROM genres
	</select>

	<insert id="insertGenre"
		parameterType="com.itwillbs.domain.UserGenresVO">
		INSERT INTO user_genres (user_id, genre_id)
		VALUES
		(#{userId}, #{genreId})
	</insert>
	
    <!-- (1) 카카오 ID로 조회 -->
    <select id="findBykakaoId" resultType="com.itwillbs.domain.MemberVO">
        SELECT * FROM users WHERE kakao_id = #{kakaoId}
    </select>

    <!-- (2) 카카오 신규 회원 생성 -->
    <insert id="insertkakaoMember" parameterType="com.itwillbs.domain.MemberVO">
        INSERT INTO users (user_id, password, kakao_id, email, nickname, username, role, profile_image)
        VALUES (#{user_id},#{password}, #{kakaoId}, #{email}, #{nickname}, #{username}, #{role}, #{profileImage})
    </insert>

    <!-- 네이버 ID로 조회 -->
    <select id="findBynaverId" resultType="com.itwillbs.domain.MemberVO">
        SELECT * FROM users WHERE naver_id = #{naverId}
    </select>

    <!-- 네이버 신규 회원 생성 -->
    <insert id="insertnaverMember" parameterType="com.itwillbs.domain.MemberVO">
        INSERT INTO users (user_id, password, naver_id, email, nickname, username, role, phone, profile_image, naver_access_token)
        VALUES (#{user_id},#{password}, #{naverId}, #{email}, #{nickname}, #{username}, #{role}, #{phone}, #{profileImage}, #{naver_access_token})
    </insert>
    
	<!-- 로그아웃시 토큰삭제 -->
	<update id="updateAccessToken">
    UPDATE users
    SET naver_access_token = #{naver_access_token}
    WHERE naver_id = #{naverId} </update>
    
     <!-- 소셜 회원가입 시 업데이트 -->
    <update id="updateAdditionalInfo">
	    UPDATE users
	    SET 
	        email = #{email},
	        phone = #{phone}
	    WHERE user_id = #{user_id}
	</update>
	
	<select id="findIdByEmail" parameterType="String" resultType="String">
    SELECT user_id
    FROM users
    WHERE email = #{email}
    </select>
    
    <select id="getMemberByIdAndEmail" resultType="com.itwillbs.domain.MemberVO">
    SELECT *
    FROM users
    WHERE user_id = #{userId} AND email = #{userEmail}
    </select>
    
    <update id="updateTemporaryPassword" parameterType="com.itwillbs.domain.MemberVO">
    UPDATE users
    SET password = #{password}
    WHERE user_id = #{user_id}
    </update>
    



</mapper>